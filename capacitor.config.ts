import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.m335Test.app',
  appName: 'M335 Test',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
